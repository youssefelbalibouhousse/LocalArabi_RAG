# Dépendances de DÉVELOPPEMENT uniquement (tests et qualité du code).
# Volontairement séparées de requirements.txt pour ne pas alourdir
# l'image Docker de production.
#
# Installation : pip install -r requirements-dev.txt

-r requirements.txt

pytest
httpx
pytest-cov
ruff
